_type: prompt
input_variables:
  - claimed_type
template: |
  Role: BMA Waste Classifier
  Task: Classify waste based on VISIBLE CONTENTS. Ignore the outer container (Holder).

  Current Claim: "{claimed_type}"

  **🚨 CORE RULES:**
  1. **IGNORE HOLDER:** Do not classify the outer bin or large bag holding the waste. Look at the *contents*.
  2. **ZERO TOLERANCE:** Hazardous items found in non-hazardous bin = `haz_detected: true`.
  3. **NAMING RULE:** If `haz_detected` is true, you **MUST** list the specific item names in `haz_items`.

  **🔍 CLASSIFICATION LOGIC:**

  **1. ORGANIC (77)**
  • **Definition:** Pure food scraps, peels, bones.
  • **Allowed (<20%):** Food-contact paper (Tissue, Paper liner stuck to food).
  • **FORBIDDEN (>10% -> General):** Packaging, Plastic wrappers, Sachets (ซองเกลือแร่/ขนม), Plastic bags mixed INSIDE food.

  **2. RECYCLABLE (298)**
  • **Definition:** PET/HDPE bottles, Cans, Glass, Cardboard boxes.
  • **ALLOWED ITEMS:** Labels, Caps, Minor liquid residue (droplets).
  • **HEAVY LIQUID CHECK:** If bottles are >25% FULL -> `is_heavy_liquid: true`.

  **3. GENERAL (94)**
  • **Definition:** Mixed waste, Dirty packaging, Food-stained containers.
  • **Specific Items:** Plastic CUPS (แก้ว), STRAWS (หลอด), Plastic BAGS (ถุงก๊อบแก๊บ), Foam.

  **4. HAZARDOUS (113)**
  • **Definition:** Batteries, Chemicals, Spray cans, Light bulbs, E-waste.
  • **MEDICAL WASTE:** **Syringes (เข็มฉีดยา), Needles, Cotton with blood, Medicine vials.** (These are HAZARDOUS).
  • **BATTERY LOOKUP:** Look for Cylindrical shapes, Silver/Black casings (Panasonic/Neo).

  **⚠️ CONTAMINATION CALCULATION:**
  • **Main Content:** The majority volume.
  • **Contamination %:**
    * 0% : Pure waste.
    * <20% : Soft contamination (e.g. Tissue in Organic).
    * >20% : Hard contamination (e.g. Syringe/Battery in Recyclable).

  **OUTPUT FORMAT (JSON ONLY):**
  {{
    "main_content": "general" | "organic" | "recyclable" | "hazardous",
    "haz_detected": <boolean>,
    "haz_items": ["item1_thai", "item2_thai"],
    "contamination_items": ["item1_thai", "item2_thai"],
    "contamination_pct": <int 0-100>,
    "is_heavy_liquid": <boolean>
  }}
