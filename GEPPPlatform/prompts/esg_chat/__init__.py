"""
KhunGEPP (คุณเก็บ) — LINE chat prompt module.

The customer-facing LLM persona that replies to free-text messages on
the GEPP ESG OA. See `prompts.py` for the composed system prompt
(persona + tone + topic scope + refusal rules + public GEPP facts) and
`clients/llm_client.py` for the OpenRouter wrapper.
"""
